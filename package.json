{
  "name": "discord-bot",
  "version": "1.0.0",
  "description": "A discord bot template",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "echo \"No Test\"",
    "start": "node .",
    "lint": "prettier --ignore-path .gitignore --write \"**/*.+(ts)\"",
    "deploy": "node dist/deploy-commands.js",
    "serve": "npm run build && npm run deploy && npm run start"
  },
  "keywords": [
    "discord-bot",
    "discord"
  ],
  "author": "FloatingComet62",
  "license": "MIT",
  "dependencies": {
    "@discordjs/builders": "^1.8.2",
    "discord.js": "^14.15.3",
    "dotenv": "^16.4.5",
    "keyv": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^20.14.1",
    "prettier": "^3.3.0",
    "typescript": "^5.4.5"
  }
}
